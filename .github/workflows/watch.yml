name: Watch Murayama FC

on:
  schedule:
    - cron: "*/5 * * * *" # 5分毎に実行
  push:

jobs:
  check_update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
      - name: Install dependencies
        run: npm install node-fetch@2
      - name: Run watch script
        run: node watchMurayama.js
